# VisionTrack: Core Computer Vision Dependencies
numpy>=1.24.0
opencv-python-headless>=4.8.0
scipy>=1.10.0
scikit-learn>=1.2.0
pandas>=2.0.0
matplotlib>=3.7.0
pytest>=7.0.0

# Pretrained Object Detection (Lightweight YOLOv8 on CPU)
# Fallback classical motion-blob & OpenCV DNN detectors are built-in if ultralytics is omitted.
ultralytics>=8.0.0
